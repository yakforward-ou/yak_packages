name: example
description: An example app for [yak_runner] package.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  yak_runner: ^0.0.1
  http: ^0.12.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true