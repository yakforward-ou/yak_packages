name: yak_runner_example
description: An example for `yak_runner`
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.1 <4.0.0'

dependencies:
  yak_runner: ^3.0.3

dev_dependencies:
  lints: ^4.0.0
