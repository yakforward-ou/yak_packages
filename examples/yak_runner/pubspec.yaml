name: example
description: An example app for `yak_runner`
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  yak_runner: ^0.3.0
 
flutter:
  uses-material-design: true